# Frontend Environment Variables

# Backend API URL
# During development: Use /api to trigger Vite proxy
# Vite proxy intercepts http://localhost:8080/api and forwards to localhost:3000/api
# This avoids CORS issues because request appears to come from same origin
VITE_API_URL=/api

# App environment
VITE_APP_ENV=development
